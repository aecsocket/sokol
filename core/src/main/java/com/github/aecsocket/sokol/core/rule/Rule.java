package com.github.aecsocket.sokol.core.rule;

public interface Rule {
    /*
    component:
      slots: {key: slot}
      stats: {...}
    slot:
      rule: ? (applies to BLUEPRINTS)
    stat map:
      rule: ? (applies to NODES)
     */
}
